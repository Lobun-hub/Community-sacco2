const nextConfig = {
  reactStrictMode: true,
  // Use Next.js static export mode (replaces removed `next export` command)
  output: "export",
  // Optional: set a custom build cache directory if needed by the host
  // distDir: '.next',
};

export default nextConfig;
